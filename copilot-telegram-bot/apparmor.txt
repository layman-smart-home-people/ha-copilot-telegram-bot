#include <tunables/global>

profile copilot-telegram-bot flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # s6-overlay v3
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /usr/lib/** mr,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Node.js
  /usr/bin/node rix,
  /usr/local/bin/node rix,
  /app/** r,
  /app/node_modules/** mr,

  # Copilot CLI binary
  /data/copilot/bin/copilot rix,
  /share/copilot-tools/copilot rix,
  /share/copilot-tools/** rw,

  # Persistent data
  /data/** rw,
  /data/copilot/** rwk,

  # Mapped volumes
  /config/** rw,
  /share/** rw,
  /media/** rw,
  /ssl/** r,

  # Network (Telegram, GitHub, Supervisor)
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,

  # Process management
  signal (send) set=(kill,term,int,hup,cont),
  signal (receive),

  # Capabilities needed
  capability net_bind_service,

  # System
  /etc/passwd r,
  /etc/group r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/nsswitch.conf r,
  /etc/ssl/** r,
  /proc/** r,
  /sys/fs/cgroup/** r,
}
